## 1. Pure debt comparison policy

- [ ] 1.1 Add pure source relevance tests for Regressed, Improved, debt-bearing Added/Removed, and MetricChanged with Watch or High on either side.
- [ ] 1.2 Prove Unchanged, Ambiguous, healthy-only Added/Removed/MetricChanged remain complete report and JSON facts but are absent from human debt links.
- [ ] 1.3 Add pure architecture and evolution tests selecting only introduced findings as Worse and removed findings as Better, with unchanged findings and Changed context excluded from human links.
- [ ] 1.4 Add one private `DebtDiffSelection` per scope with three unique typed lists for trusted source debt, introduced/removed architecture findings, and introduced/removed evolution findings; derive meaningful `DebtDiffCounts` only from source IDs.
- [ ] 1.5 Prove source-only, architecture-only, history-only, and mixed selections keep source `QUALITY`, `AREAS`, and discover separate; architecture/history never enter combined counts, and no-debt checks all three lists plus real changed-source gaps.
- [ ] 1.6 Prove fixture, generated, and recovered/advisory comparisons remain JSON-only in diff default, diff `--all`, and diff path; never affect selection/counts/quality/areas/discover/no-debt; preserve test/example/benchmark eligibility; and narrow only the earlier general codebase role/advisory detail policy.
- [ ] 1.7 Prove a recovered-only issue makes diff default, diff `--all`, and diff path exactly `No debt changed in checked files.` plus one exact grouped warning with no optional detail, while JSON retains the comparison unchanged.
- [ ] 1.8 Add report/index integrity tests rejecting a repeated typed ID within each of the three selection lists while allowing one link in each separate ancestor scope.

## 2. Analysis-owned links and stable selection

- [ ] 2.1 Build private nonserialized per-scope `DebtDiffSelection` objects with separate trusted source, architecture, and evolution typed ID lists during analysis/report aggregation.
- [ ] 2.2 Reserve link storage and aggregate each selected comparison once per applicable scope without rating-loop allocation, N+1 lookup, or repeated ancestry work.
- [ ] 2.3 Order links by Worse, Better, Changed, then existing stable rank, path, span, and typed ID tie-breakers.
- [ ] 2.4 Prove terminal consumes the completed selection for default and `--all` without trust filtering, reclassification, count changes, global comparison scans, identity mapping, second selected-ID collection, or silent repair.
- [ ] 2.5 Prove existing complete links, comparisons, directions, counts, report facts, and exact JSON version-3 bytes remain unchanged.

## 3. Debt-focused terminal output

- [ ] 3.1 Render default `QUALITY`, source `AREAS`, and source discover only from nonzero source `DebtDiffCounts`; prove source-only, architecture-only, history-only, and mixed section separation with no combined count.
- [ ] 3.2 Render at most five source-debt areas, first three source comparisons, three introduced/removed architecture findings, three introduced/removed history findings, selected warnings, and one source discover command; prove plain no-debt checks all three lists and real gaps.
- [ ] 3.3 Make `--all` remove only trusted human-debt limits while recovered comparisons remain JSON-only and `DebtDiffCounts`, `QUALITY`, `AREAS`, discover, and no-debt math remain selection-owned; make paths change scope only.
- [ ] 3.4 Aggregate unique selected changed files once across read fail, failed/recovered parse, and ambiguous unit match; render exactly `<Warning glyph> 1 changed file could not be checked.` or `<Warning glyph> <count> changed files could not be checked.` with no raw reason/per-unit row; scope architecture/history warnings as accepted.
- [ ] 3.5 Render every numerically changed measurement as before-to-after for Regressed, Improved, and relevant MetricChanged, including mixed direction; for Added/Removed render every existing-side measurement whose individual signal is Watch/High and no Healthy metric, plus the exact side location/line.
- [ ] 3.6 Render anonymous identities as `container · closure|lambda|kind`, `filename · kind`, or `filename · template`, place exact `path:line` next, retain real named identities, reject angle placeholders and anonymous `::kind`, and prove repeated anonymous units differ by line without JSON changes.
- [ ] 3.7 Omit empty AREAS, FINDINGS, ARCHITECTURE, HISTORY, warning, and discover sections in every diff scope and mode.

## 4. Documentation and exact public proof

- [ ] 4.1 Update CLI help and README so diff means trusted primary/test/example/benchmark debt movement, fixture/generated/recovered comparisons stay JSON-only in default/`--all`/path, codebase behavior stays unchanged, path means scope only, and JSON remains complete.
- [ ] 4.2 Update `ARCHITECTURE.md` for one private analysis-owned trusted selection object per scope, `DebtDiffCounts`, integrity audits, direct rendering without trust policy, and unchanged serialized facts.
- [ ] 4.3 Add guarded exact dense repository, package, directory, and file diff snapshots for default and `--all` at widths 120, 100, 80, and 50.
- [ ] 4.4 Cover explicit public cases for clean worktree; docs/config-only; healthy Added; healthy Removed; retained Unchanged; Ambiguous; fixture-only Watch/High; generated-only Watch/High; recovered-only Watch/High; eligible test/example/benchmark debt; High Added; Watch Added; High Removed; Watch Removed; multi-signal Added; multi-signal Removed with a Healthy metric; Regressed; Improved; same-rating Watch MetricChanged; same-rating High MetricChanged; mixed-direction relevant changes; introduced cycle; removed cycle; introduced coupling; removed coupling; Changed-only edge/context; and selected paths, across default and `--all` at 120/100/80/50, plus exact fixture/generated/recovered path flows.
- [ ] 4.5 Cover source-only/architecture-only/history-only/mixed separation, stable direction/rank, source-only counts/navigation, all measurement cases, locations/lines, exact middle-dot identities, both no-debt states, exact unique-changed-file warnings, discover, and empty-section absence.
- [ ] 4.6 Audit all human snapshots/help/README output for absence of unchanged, ambiguous cards, healthy-only comparisons, ordinary relation changes, history context, raw/internal text, label-only added/removed/changed cards, `<closure 1177>`, and `<template>`.
- [ ] 4.7 Prove default public repository diff and selected package/directory/file results have at most 60 nonempty lines at widths 120/100/80 and at most 100 at width 50; impose no budget on `--all`.

## 5. Unchanged interfaces, work, and release proof

- [ ] 5.1 Prove serial and automatic terminal bytes match for every revised diff flow.
- [ ] 5.2 Validate unchanged exact JSON version-3 bytes/schema/indexes/privacy, every comparison, field, direction, complete count, status, stream, and work total.
- [ ] 5.3 Prove one visit per completed selection ID, zero renderer trust filtering/reclassification/global scans/second collections, zero project work, zero normal safety-fallback shortening, and unchanged allocation/resource limits.
- [ ] 5.4 Pass focused source, architecture, evolution, report, renderer, CLI, selection, snapshot, documentation, API, allocation, and performance checks.
- [ ] 5.5 Review self read-only and record only that a clean diff stops at exact no-debt output, a changed diff leads with debt direction plus measurement/location explanation, selected default paths meet budgets, JSON remains complete, and work stays unchanged.
- [ ] 5.6 Review the private mixed application read-only and record only the same separate clean-diff, changed-diff, selected-path, completeness, and work outcome classes.
- [ ] 5.7 Review the private Rust workspace read-only and record only the same separate clean-diff, changed-diff, selected-path, completeness, and work outcome classes.
- [ ] 5.8 Commit no raw private output, names, paths, source, identities, history, or repository facts in snapshots, logs, or evidence.
- [ ] 5.9 Pass formatting, Clippy, workspace tests, dependency/API checks, licenses, strict OpenSpec validation, and final diff check.
- [ ] 5.10 Implement only after both earlier terminal changes are reviewed and archived; review and archive this change before resuming `prepare-first-release`.
