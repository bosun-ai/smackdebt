## 1. Pure selection policy

- [ ] 1.1 Add pure default-selection tests for repository, package, directory, and file scopes with five areas, three ranked source findings, three architecture findings with closed witnesses, three actionable history findings, grouped warnings, and one discover command.
- [ ] 1.2 Add pure `--all` tests that remove limits only for debt-bearing areas, retained Watch/High source findings with existing qualifiers, architecture findings and witnesses, actionable evolution findings, and real failed/recovered diagnostics.
- [ ] 1.3 Add pure absence tests for raw resolved edges, ownership, external, unmatched, ambiguous, weak/explained history, activity, concentration, history coverage, healthy rows, empty sections, and repeated linked-ID rows in default, `--all`, and path views.
- [ ] 1.4 Select human history only through existing actionable evolution-finding IDs and prove default takes three while `--all` takes every linked ID in stable order.
- [ ] 1.5 Prove role and trust remain display qualifiers and do not enter verdict arithmetic or terminal reclassification.
- [ ] 1.6 Add a negative report/index integrity test for the workspace-architecture and report-schema-v3 contracts that inserts a duplicate typed ID into one scope owning list and fails before rendering; prove accepted current fixtures are already unique and retain exact JSON bytes.

## 2. Terminal selection and wording

- [ ] 2.1 Apply the same default relevance policy to repository, package, directory, and file selections without treating a path as a detail switch.
- [ ] 2.2 Implement useful-debt `--all` selection through existing child, finding, architecture-finding, evolution-finding, and diagnostic links.
- [ ] 2.3 Trust completed unique typed link lists and render each linked ID once within its owning section; allow the same fact to appear once in each separately rendered ancestor scope.
- [ ] 2.4 Keep source finding commit counts, closed witness evidence, actionable coupling shared/union commit counts and similarity, and replace the final human phrase with `not linked in code`.
- [ ] 2.5 Retain only the grouped architecture resolution warning in human output; do not render raw relationship rows in any scope or detail mode.
- [ ] 2.6 Omit optional headings and sections when their selected row set is empty.

## 3. One-pass and unchanged interfaces

- [ ] 3.1 Iterate each applicable existing report link once for selection and reject N+1 or repeated global-table scans per displayed row.
- [ ] 3.2 Borrow or stream selected rows without a de-dup set, second selected-ID collection, identity mapping, second report, large path/finding clones, filesystem work, Git work, parsing, analysis, or worker scheduling.
- [ ] 3.3 Prove accepted current fixtures already have unique owning link lists and retain exact report facts, JSON version-3 bytes/schema/indexes/privacy, analysis, rank, CLI flags, exits, serial/automatic behavior, work counters, allocations, and resource profiles.
- [ ] 3.4 Add focused renderer allocation and link-visit regressions for dense default and `--all` repository, package, directory, and file selection, proving one visit per linked ID and no de-dup allocation.

## 4. Documentation and exact public proof

- [ ] 4.1 Update CLI help and README so `--all` means all useful debt and JSON is the complete retained report.
- [ ] 4.2 Update README and `ARCHITECTURE.md` for same-scope relevance, path behavior, selected families, JSON-only families, analysis-owned unique typed link lists, index audits, and one-pass borrowed rendering without de-duplication.
- [ ] 4.3 Add guarded exact dense repository, package, directory, and file snapshots for default and `--all` at widths 120, 100, 80, and 50.
- [ ] 4.4 Assert exact limits, stable order, closed witnesses, qualifiers, actionable history evidence ending `not linked in code`, grouped warnings, one discover command, and no empty sections.
- [ ] 4.5 Audit every human snapshot/help/README result for absence of raw relationships, weak/explained history, activity, concentration, history coverage/processing, healthy rows, repeated linked-ID rows, and the old dependency phrase.
- [ ] 4.6 Prove reviewed dense default public codebase and selected package/directory/file path results have at most 60 nonempty lines at widths 120, 100, and 80 and at most 100 at width 50; impose no line budget on `--all`.

## 5. Complete and workload proof

- [ ] 5.1 Prove serial and automatic terminal bytes match for every revised public flow.
- [ ] 5.2 Validate unchanged exact JSON bytes, version-3 schema, indexes, completeness, privacy, status, streams, and work totals for the same fixtures.
- [ ] 5.3 Pass focused renderer, CLI, selection, snapshot, documentation, API, allocation, and performance checks.
- [ ] 5.4 Review self read-only and record only whether selected default paths meet line budgets and whether `--all` includes every useful-debt row with zero excluded raw/context rows, plus aggregate relevance and one-visit completed-link outcomes.
- [ ] 5.5 Review the private mixed application read-only and record only the same aggregate outcome classes.
- [ ] 5.6 Review the private Rust workspace read-only and record only the same aggregate outcome classes.
- [ ] 5.7 Commit no raw private output, names, paths, source, identities, or history in snapshots, logs, or evidence.
- [ ] 5.8 Pass formatting, Clippy, workspace tests, dependency and API checks, licenses, strict OpenSpec validation, and final diff check.
- [ ] 5.9 Implement only after `make-terminal-verdict-clear` is reviewed and archived; review and archive this change before implementing `make-diff-output-debt-focused`; keep `prepare-first-release` blocked until all three are archived.
