## 1. Git history stream

- [ ] 1.1 Add compact commit, rename, textual churn, and history coverage values
  in `smackdebt-git`.
- [ ] 1.2 Parse one streamed non-merge history process with generated commits,
  renames, binary changes, mailmap data, shallow history, and malformed-record
  fixtures.
- [ ] 1.3 Follow rename aliases to selected current files and report excluded or
  broken history without guessing.

## 2. Evolutionary policy

- [ ] 2.1 Add typed file and package history values to `smackdebt-analysis`.
- [ ] 2.2 Implement one focused module each for churn, package change coupling,
  contributor concentration, and evolutionary comparison.
- [ ] 2.3 Prove exact package touch deduplication, churn sums, pair counts,
  Jaccard operands, and concentration operands with pure tests.
- [ ] 2.4 Join retained coupling pairs to static package edges and add Watch
  findings only for recurring unexplained cross-package coupling.
- [ ] 2.5 Prove contributor identities cannot enter report values or diagnostics.

## 3. Project composition

- [ ] 3.1 Stream history through project orchestration without retaining commit
  messages, source text, or complete per-commit file lists.
- [ ] 3.2 Keep repositories without usable history analyzable and attach exact
  availability and coverage diagnostics.
- [ ] 3.3 Attach historical context to changed files and packages without
  claiming the worktree changed history.
- [ ] 3.4 Measure one history process, controlled pair storage, and stable output
  with one worker and automatic parallelism.

## 4. Reports and output

- [ ] 4.1 Add separate `EVOLUTION` and diff-context terminal sections with
  history coverage, churn, coupling, and concentration.
- [ ] 4.2 Extend JSON version 2 with flat history coverage, file history, package
  history, coupling, concentration, and evolutionary finding tables.
- [ ] 4.3 Add terminal and JSON snapshots proving exact values and the absence of
  fixture author names, addresses, raw fields, and internal identities.

## 5. Acceptance and documentation

- [ ] 5.1 Add generated repositories for renames, repeated coupling, static-edge
  explanation, binary changes, missing history, and dense package commits.
- [ ] 5.2 Add black-box codebase and diff tests proving source, static
  architecture, and evolution remain separate but work in one report.
- [ ] 5.3 Update `ARCHITECTURE.md`, `README.md`, acceptance examples, and schema
  documentation with history meaning, privacy, and limitations.
- [ ] 5.4 Run formatting, Clippy, workspace tests, strict OpenSpec validation,
  dependency checks, API snapshots, license checks, acceptance snapshots,
  allocation checks, performance workloads, and diff checks.

