## 1. Terminal vocabulary and relevance

- [x] 1.1 Add the exact private glyph vocabulary, one-cell checks, U+EC3F rejection, and glyph-only colors.
- [x] 1.2 Always show `QUALITY`; show `AREAS` only for several affected areas with at most five rows; show the first three ranked `FINDINGS`.
- [x] 1.3 Show `ARCHITECTURE` only for findings with default cycle witnesses and `HISTORY` only for the three strongest actionable findings.
- [x] 1.4 Replace discover guidance with the Discover glyph and command only.
- [x] 1.5 Remove repeated bars, summary percentages and ratios, healthy counts, severity words, edge totals, weak pairs, processing totals, and named internal phrases while retaining actionable coupling evidence.

## 2. Detail and human text

- [x] 2.1 Keep all useful findings and relevant relationships in `--all` without healthy rows or internal processing facts.
- [x] 2.2 Preserve relevant incoming and outgoing relationships in path views.
- [x] 2.3 Group repeated parser and file warnings by default and retain affected-file detail in `--all` and path views.
- [x] 2.4 Use the accepted simple history, rename, unresolved-import, and ambiguous-import sentences with correct plural forms.
- [x] 2.5 Simplify CLI help and errors while preserving statuses and stdout/stderr behavior.
- [x] 2.6 Call logical lines `statements` in human output while preserving the analysis value and JSON field.

## 3. Documentation and public proof

- [x] 3.1 Rewrite README examples and terminal guidance for the exact sections, glyphs, relevance rules, Nerd Font requirement, and simple human language.
- [x] 3.2 Add exact codebase, diff, package, directory, and file snapshots at widths 120, 80, and 50.
- [x] 3.3 Prove forced and disabled color, `NO_COLOR`, redirect behavior, ANSI stripping, truncation, alignment, exact code points, and one-cell glyph width.
- [x] 3.4 Add human-text audits for removed labels, severity words, processing facts, and forbidden phrases across terminal, help, errors, and README.
- [x] 3.5 Prove JSON bytes, exit behavior, ranking, serial/automatic equality, live work totals, and analysis facts remain unchanged.

## 4. Resource and workload evidence

- [x] 4.1 Pass focused renderer, CLI, snapshot, documentation, API, allocation, and performance checks.
- [x] 4.2 Review self read-only: important debt leads and empty optional sections are absent.
- [x] 4.3 Review the private mixed application read-only: primary application findings lead and generated Rails schema stays outside default debt.
- [x] 4.4 Review the private Rust workspace read-only: useful Rust findings lead and weak history and graph facts are absent.
- [x] 4.5 Commit only privacy-safe workload families and outcome categories; never raw private output, paths, source, identities, or history.
- [x] 4.6 After the reviewed implementation commit, record clean public and aggregate release evidence from one revision with `workspace_dirty` false.
- [x] 4.7 Pass formatting, Clippy, workspace tests, architecture/API gates, licenses, strict OpenSpec validation, and final diff check.
