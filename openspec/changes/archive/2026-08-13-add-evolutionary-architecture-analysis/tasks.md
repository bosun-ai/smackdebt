## 1. Git history stream

- [x] 1.1 Add compact commit, rename, textual churn, and history coverage values
  in `smackdebt-git`.
- [x] 1.2 Parse one streamed non-merge history process with generated commits,
  renames, binary changes, mailmap data, shallow history, and malformed-record
  fixtures.
- [x] 1.3 Follow rename aliases to selected current files and report excluded or
  broken history without guessing.

## 2. Evolutionary policy

- [x] 2.1 Add typed file and package history values to `smackdebt-analysis`.
- [x] 2.2 Implement one focused module each for churn, package change coupling,
  contributor concentration, and evolutionary comparison.
- [x] 2.3 Prove exact package touch deduplication, churn sums, pair counts,
  Jaccard operands, and concentration operands with pure tests.
- [x] 2.4 Join retained coupling pairs to static package edges and add Watch
  findings only for recurring unexplained cross-package coupling.
- [x] 2.5 Prove contributor identities cannot enter report values or diagnostics.

## 3. Project composition

- [x] 3.1 Stream history through project orchestration without retaining commit
  messages, source text, or complete per-commit file lists.
- [x] 3.2 Keep repositories without usable history analyzable and attach exact
  availability and coverage diagnostics.
- [x] 3.3 Attach historical context to changed files and packages without
  claiming the worktree changed history.
- [x] 3.4 Measure one history process, controlled pair storage, and stable output
  with one worker and automatic parallelism.

## 4. Reports and output

- [x] 4.1 Add separate `EVOLUTION` and diff-context terminal sections with
  history coverage, churn, coupling, and concentration.
- [x] 4.2 Extend JSON version 2 with flat history coverage, file history, package
  history, coupling, concentration, and evolutionary finding tables.
- [x] 4.3 Add terminal and JSON snapshots proving exact values and the absence of
  fixture author names, addresses, raw fields, and internal identities.

## 5. Acceptance and documentation

- [x] 5.1 Add generated repositories for renames, repeated coupling, static-edge
  explanation, binary changes, missing history, and dense package commits.
- [x] 5.2 Add black-box codebase and diff tests proving source, static
  architecture, and evolution remain separate but work in one report.
- [x] 5.3 Update `ARCHITECTURE.md`, `README.md`, acceptance examples, and schema
  documentation with history meaning, privacy, and limitations.
- [x] 5.4 Run formatting, Clippy, workspace tests, strict OpenSpec validation,
  dependency checks, API snapshots, license checks, acceptance snapshots,
  allocation checks, performance workloads, and diff checks.
