## 1. Verdict and area policy

- [ ] 1.1 Add pure policy tests for grouped checked digits, unsigned widened integer arithmetic without a new report field, nonnegative nearest-tenth percentage, exact halfway-up rounding such as 1 of 16 to 6.3%, checked greater than zero with zero attention, checked zero, and non-zero High and Watch counts.
- [ ] 1.2 Render the exact two-line non-empty `QUALITY` verdict, the sole `Nothing was checked.` empty verdict, and remove the old rated-unit, need-attention, and duplicate icon totals.
- [ ] 1.3 Remove area rates, shares, percentages, and bars while preserving exact status counts and stable severity-led order.
- [ ] 1.4 Prove report facts, distribution values, ordering keys, and JSON version-3 bytes remain unchanged.

## 2. Complete-analysis warnings

- [ ] 2.1 Define real source gaps as read failure, failed parse, or recovered/advisory parse excluded from health.
- [ ] 2.2 Exclude only fixture and generated role policy from coverage warnings; keep primary, test, example, and benchmark source in the default verdict.
- [ ] 2.3 Group real gaps in default root, package, and directory views; name every relevant failed or recovered file once in `--all`; name an explicitly selected affected file; and leave default directory file-list detail unchanged for `make-terminal-detail-relevant`.
- [ ] 2.4 Combine unmatched and ambiguous imports into the exact Warning-glyph `<count> import(s) could not be followed.` row, separate from source coverage and without command, status, parser, or process text.
- [ ] 2.5 Add pure and CLI regressions for real gaps, fixture/generated exclusions, grouped counts, detail de-duplication, and the exact architecture warning.

## 3. Content-aware terminal writing

- [ ] 3.1 Replace fixed width-tier row choice with measured compact-or-stacked row writing.
- [ ] 3.2 Middle-shorten paths and identities within computed budgets while retaining measurements, counts, cycle closure, history commit evidence, dependency state, commands, and identities.
- [ ] 3.3 Keep the final writer fallback for unexpected input and expose test-only hit evidence at that seam.
- [ ] 3.4 Add exact default codebase, path, and diff snapshots at widths 120, 100, 80, and 50.
- [ ] 3.5 Prove every ANSI-stripped line fits its requested Unicode display width and every reviewed flow records zero fallback hits; keep fallback use to a synthetic overflow test.

## 4. Errors and documentation

- [ ] 4.1 Add byte-exact missing-path, missing-ref, and `--all --json` acceptance tests for required status, empty stdout, one newline-terminated stderr line, and no usage or help tail.
- [ ] 4.2 Implement the exact `smackdebt:` error bytes from original user inputs without absolute paths, operating-system codes, Git command/status/fatal text, or added usage.
- [ ] 4.3 Update README examples and guidance for the verdict, areas, real coverage gaps, the exact architecture warning, content-aware widths, and errors.
- [ ] 4.4 Update `ARCHITECTURE.md` for presentation arithmetic, content measurement, stacking, middle shortening, fallback ownership, and unchanged JSON.
- [ ] 4.5 Update checked documentation examples and text audits to reject old quality, area-rate/share, width-tier, and leaked implementation wording.

## 5. Public and workload proof

- [ ] 5.1 Prove glyph-only color and ANSI-stripped text equality for every revised public flow.
- [ ] 5.2 Prove JSON version-3 bytes, report facts, rank, status, streams, serial/automatic bytes, live work totals, allocations, and resource profiles remain unchanged.
- [ ] 5.3 Review self read-only and record only aggregate verdict, coverage, architecture-warning, and width outcomes.
- [ ] 5.4 Review the private mixed application read-only and record only the same aggregate outcome classes.
- [ ] 5.5 Review the private Rust workspace read-only and record only the same aggregate outcome classes.
- [ ] 5.6 Commit no raw private output, names, paths, source, identities, or history in snapshots, logs, or evidence.
- [ ] 5.7 Pass focused renderer, CLI, snapshot, documentation, API, allocation, and performance checks.
- [ ] 5.8 Pass formatting, Clippy, workspace tests, dependency and API checks, licenses, strict OpenSpec validation, and final diff check.
- [ ] 5.9 Permit all three changes to be authored now; archive this change before implementing `make-terminal-detail-relevant`, archive that change before implementing `make-diff-output-debt-focused`, and keep `prepare-first-release` blocked until all three are archived.
